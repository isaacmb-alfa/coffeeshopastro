---
interface Props {
  icon: string;
  text: string;
}
const { icon, text } = Astro.props;
---

<div class="flex flex-col items-center py-10 space-y-5">
  <img alt={`Icono`} class="max-w-52" src={icon} />
  <h3 class="text-4xl text-coffee-800 text-center font-secondary">{text}</h3>
</div>
